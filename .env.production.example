# Production Environment Variables for Frontend Angular SSR

# API Backend URL (used by SSR server proxy)
API_URL=https://api.onlinestore.sn

# Node Environment
NODE_ENV=production

# Server Port
PORT=4000
